## 二叉树递归心得

```cpp
一般来说，DFS 的递归边界是空节点。在什么情况下，要额外把叶子节点作为递归边界？
在什么情况下，DFS 需要有返回值？什么情况下不需要有返回值？
在什么情况下，题目更适合用自顶向下的方法解决？什么情况下更适合用自底向上的方法解决？
什么情况下选择前序、中序、后序递归

根据以上几个问题总结树的递归心得，并作相关拓展
```

这几个问题直击树形 DP 和二叉树递归的核心本质。理解了这几个问题，基本上就能破解 90% 以上的二叉树递归题。

下面为你系统总结这四个问题，并作相关拓展，形成一份“二叉树递归心法”。

---

### 一、 递归边界：何时用空节点？何时额外用叶子节点？

**1. 默认情况：空节点作为边界**
*   **原因**：空节点是树结构的最底层天然屏障。使用 `if (node == nullptr) return;` 可以统一处理所有叶子节点的左右指针，避免在访问 `node->left->val` 时发生空指针异常。
*   **适用场景**：绝大多数遍历、统计、修改类题目（如求最大深度、翻转二叉树）。

**2. 额外把叶子节点作为边界的情况**
*   **核心动机：为了避免“重复计算”或“逻辑重叠”**。叶子节点的左右子节点都是 `null`，如果只在 `null` 处处理逻辑，叶子节点会被它的左右空子节点各触发一次，往往导致逻辑执行两次。
*   **典型场景**：
    1.  **路径问题**（如求根到叶路径总和）：
        如果只在 `null` 判断，当遍历到叶子节点的左空节点时，会计算一次路径和；遍历到右空节点时，又计算一次。这就重复了。此时需要提前在叶子节点处拦截：`if (!node->left && !node->right) { /* 处理路径 */ return; }`
    2.  **寻找特定节点**（如找左叶子节点之和）：
        判断一个节点是不是“左叶子”，无法在当前节点完成，必须在它的父节点判断（即 `node->left && !node->left->left && !node->left->right`）。这时把“父节点是叶子节点的上一级”作为边界条件更直接。
    3.  **剪枝优化**：
        如果题目明确叶子节点具有特殊属性，提前在叶子节点截断递归，可以减少一半的递归调用栈（不用再去走两个 `null`）。

---

### 二、 返回值：何时需要？何时不需要？

理解返回值的核心在于**“信息流动的方向”**。

**1. 不需要返回值（`void`）**
*   **场景**：**自顶向下**传递信息，或者只需要做“副作用”（修改树结构、收集全局结果）。
*   **特点**：通过**函数参数**把父节点的状态传给子节点；通过**全局变量/引用参数**收集结果。
*   **例子**：
    *   打印/收集遍历序列。
    *   寻找和为某值的路径（把当前路径 `path` 和当前和 `sum` 作为参数往下传）。

**2. 需要返回值（非 `void`）**
*   **场景**：**自底向上**汇报信息，子树需要把计算结果交给父节点去汇总。
*   **特点**：父节点的状态依赖于子节点返回的结果。
*   **例子**：
    *   求树的最大深度：`return 1 + max(depth(left), depth(right));`（子树汇报深度，父节点加 1）。
    *   判断平衡二叉树：子树汇报自己的高度，如果子树本身不平衡，汇报 -1，父节点收到 -1 直接向上传递。
    *   最近公共祖先（LCA）：子树汇报是否找到了 p 或 q，父节点根据左右子树的汇报情况决定自己是不是 LCA。

**总结口诀**：**向下传参，向上返值。**

---

### 三、 自顶向下 vs 自底向上

这其实是上一问的延伸，决定了你写递归的架构。

**1. 自顶向下**
*   **逻辑**：先处理当前节点，再递归左右子树。父节点的信息像水流一样向下灌溉。
*   **适用场景**：当前节点的计算**不依赖**子树的结果，只依赖父节点传下来的状态。
*   **典型题目**：前序遍历、求根到叶节点路径、从根节点出发的搜索。

**2. 自底向上**
*   **逻辑**：先递归左右子树，拿到子树的结果后，再处理当前节点。
*   **适用场景**：当前节点的计算**完全依赖**左右子树的结果。这就是著名的**“树形 DP”**思想。
*   **典型题目**：后序遍历、计算树的直径（左右子树高度之和）、判断子树结构是否相同、计算最大路径和。
*   **优势**：每个节点只访问一次，天然避免了重复计算（很多用自顶向下需要 $O(N^2)$ 的题，用自底向上可以优化到 $O(N)$）。

---

### 四、 如何选择前序、中序、后序？

选择遍历顺序的本质，是**选择“当前节点与它左右子树处理的先后时机”**。

| 遍历方式 | 时机                                 | 代码位置                                    | 适用场景                                                     |
| :------- | :----------------------------------- | :------------------------------------------ | :----------------------------------------------------------- |
| **前序** | 根 $\rightarrow$ 左 $\rightarrow$ 右 | `处理当前节点`<br>`f(left)`<br>`f(right)`   | **进入节点时做事**。<br>1. 自顶向下传递信息。<br>2. 构造/复制二叉树（必须先有根才能挂子树）。<br>3. 搜索剪枝（不满足条件直接 return，不进子树）。 |
| **中序** | 左 $\rightarrow$ 根 $\rightarrow$ 右 | `f(left)`<br>``处理当前节点``<br>`f(right)` | **处理有大小关系的数据**。<br>1. 几乎只在**二叉搜索树（BST）**中使用。<br>2. BST 的中序遍历是有序数组，可用来：验证 BST、找第 K 小、恢复 BST、累加树（反向中序：右根左）。 |
| **后序** | 左 $\rightarrow$ 右 $\rightarrow$ 根 | `f(left)`<br>`f(right)`<br>`处理当前节点`   | **离开节点时做事**。<br>1. 自底向上汇总信息（树形 DP）。<br>2. **修改/删除树结构**（如你上一个删节点的问题：必须等子树处理完断开连接后，才能安全处理当前节点）。<br>3. 计算子树统计量（高度、节点数）。 |

---

### 五、 拓展：二叉树递归心法与高阶技巧

掌握了上述四点，我们可以总结出几条树递归的“心法”：

#### 1. 将大树拆解为“单节点职责”
写递归时，**永远不要在脑子里展开完整的递归树**，那会绕晕你。你只需要思考：
*   **宏观角度**：我这个函数要完成什么任务？（假设它能完美完成对左右子树的任务）
*   **微观角度**：对于当前这一个节点 `root`，我需要做什么？我需要从左子树要什么信息？从右子树要什么信息？怎么把这三个信息合并？

#### 2. 回溯与树的 DFS 的关系
树的 DFS 本质上就是一种特殊的回溯。
*   当你用自顶向下收集路径时（如 `path.push_back(node->val)`），在递归完左右子树后，**必须进行回溯**（`path.pop_back()`），恢复状态。
*   **心法**：前序位置做选择（往下传），后序位置撤销选择（往回退）。

#### 3. 引入虚拟节点（Dummy Node）
在处理涉及根节点变更的题目时（如你上一个删节点的题，或者合并两个有序链表的树版本），引入一个虚拟根节点指向真正的 `root`，可以避免对“根节点是否需要特殊处理”的边界判断，让所有节点平等地走“被父节点检查”的逻辑。

#### 4. 树形 DP 通用模板
遇到树的最优解问题，直接套用自底向上的后序遍历：
1.  **定义状态**：明确递归函数需要向上返回什么（通常是一个结构体或数组，包含多个维度的信息）。
2.  **Base case**：`null` 节点返回什么基础状态。
3.  **后序汇总**：通过左右子树返回的状态，计算当前节点的状态，并更新全局最优解（如果需要），然后返回当前状态。

**举例（求二叉树最大路径和）**：
*   **状态**：以当前节点为端点，向下延伸的最大单臂路径和。
*   **汇总**：当前节点的最大单臂路径 = `max(0, left_gain) + max(0, right_gain) + node->val`。
*   **全局更新**：在汇总时，顺便用 `left_gain + right_gain + node->val` 更新全局最大路径和。

### 结语
树的递归题目千变万化，但底层逻辑逃不出**信息流向（上下）**、**处理时机（前中后）**和**边界控制（空/叶）**这三个维度的组合。遇到题目先想清楚这三个问题，代码自然水到渠成。