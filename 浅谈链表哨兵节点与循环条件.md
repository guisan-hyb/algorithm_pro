## 浅谈链表哨兵节点与循环条件

在链表算法题中，**哨兵节点**和**循环条件**是两个最核心的工程技巧。理解它们的本质，能帮你避免 90% 的空指针异常。

---

### 一、 什么情况下要用到哨兵节点？

**核心原则：只要操作可能改变头节点，或者需要统一对第一个节点的处理逻辑，就用哨兵节点。**

哨兵节点的作用是**消除边界特判**。如果不使用哨兵节点，你在进行插入或删除时，必须写 `if (head == nullptr)` 或 `if (要删除的是头节点)` 的特殊判断。加了哨兵节点后，头节点变成了 `dummy->next`，它和链表中的其他节点地位完全平等，不再需要特判。

#### 典型场景：

1. **删除节点（尤其是头节点可能被删除时）**
   * **例子**：LeetCode 203. 移除链表元素（删除链表中所有值为 val 的节点）。
   * **原因**：如果头节点的值就是 val，你需要把 head 往后移。如果没有 dummy，你需要用 while 循环不断检查新的头节点；有了 dummy，头节点只是 `prev->next`，统一用 `prev->next = prev->next->next` 即可。

2. **合并两个链表 / 重组链表**
   * **例子**：LeetCode 21. 合并两个有序链表；LeetCode 24. 两两交换链表中的节点。
   * **原因**：你需要一个“尾巴指针”来不断 `tail->next = ...` 拼接新链表。如果不用 dummy，第一个节点你需要单独赋值给 head，后续节点才能用 `tail->next` 拼接。用 dummy 后，第一个节点也可以用 `tail->next` 拼接，逻辑完全统一。

3. **局部反转链表**
   * **例子**：LeetCode 92. 反转链表 II（反转中间一段）。
   * **原因**：如果 `left = 1`，即从头开始反转，你需要特判新的头节点是谁。用 dummy 后，反转区间的前一个节点永远存在（至少是 dummy），新头节点永远是 `dummy->next`。

**C++ 标准写法模板：**
```cpp
ListNode* dummy = new ListNode(0); // 值无所谓，随便写
dummy->next = head;
// ... 一顿操作 ...
ListNode* result = dummy->next;
delete dummy; // C++ 养成好习惯，防止内存泄漏（算法题里不写一般也不会报错，但面试加分）
return result;
```

---

### 二、 循环条件：`while (node != nullptr)` vs `while (node->next != nullptr)`

**核心原则：取决于你在循环体里，需要访问当前节点，还是需要访问下一个节点。**

#### 1. 什么时候用 `while (node != nullptr)`？
**当你只需要处理当前节点本身时。**
此时循环体内部不会访问 `node->next`（或者即使访问，在最后一次循环时 `node` 是尾节点，`node->next` 是 `nullptr`，你也不会对 `nullptr` 进行解引用操作）。

* **场景 A：遍历读取 / 求和 / 计数**
  ```cpp
  while (cur != nullptr) {
      sum += cur->val;
      cur = cur->next;
  }
  ```
* **场景 B：寻找某个特定值的节点**
  ```cpp
  while (cur != nullptr && cur->val != target) {
      cur = cur->next;
  }
  // 此时 cur 要么是 nullptr（没找到），要么是目标节点
  ```

#### 2. 什么时候用 `while (node->next != nullptr)`？
**当你需要以当前节点为“跳板”，去操作/访问下一个节点时。**
换句话说，你的循环体逻辑强依赖于 `node->next` 的存在。如果写 `while (node != nullptr)`，然后在里面直接用 `node->next->val`，当 `node` 是尾节点时，`node->next` 是 `nullptr`，访问 `nullptr->val` 就会报段错误。

* **场景 A：需要找到尾节点（让你停在最后一个节点上）**
  ```cpp
  while (cur->next != nullptr) {
      cur = cur->next;
  }
  // 循环结束时，cur 正好是尾节点！如果是 cur != nullptr，结束时 cur 是 nullptr，你反而丢了尾节点。
  ```
* **场景 B：删除下一个节点**
  ```cpp
  // 删除 cur 后面的那个节点
  while (cur->next != nullptr) { // 必须保证下一个节点存在才能删
      if (cur->next->val == target) {
          cur->next = cur->next->next;
      } else {
          cur = cur->next;
      }
  }
  ```
* **场景 C：成对处理节点（两两交换、快慢指针等）**
  ```cpp
  // 比如两两交换，你需要操作 cur->next 和 cur->next->next
  while (cur->next != nullptr && cur->next->next != nullptr) {
      // 这样就能保证 cur->next 和 cur->next->next 绝对安全，不会解引用空指针
  }
  ```

---

### 三、 终极总结与避坑指南

| 技巧                       | 判断依据                                               | 常见错误                                                     |
| :------------------------- | :----------------------------------------------------- | :----------------------------------------------------------- |
| **用不用 Dummy**           | 头节点会不会变？是否需要统一首次插入/删除的逻辑？      | 用了 dummy 但最后 `return head`（head 可能已经被删了，必须 `return dummy->next`） |
| **`cur != nullptr`**       | 我只关心当前节点 `cur` 本身                            | 循环结束时 `cur` 是 `nullptr`，如果此时想取尾节点信息会空指针异常 |
| **`cur->next != nullptr`** | 我要操作/访问 `cur->next`，或者我想让 `cur` 停在尾节点 | 调用前没有判空！如果链表本身为空，`cur` 是 `nullptr`，判断 `cur->next` 会直接段错误！ |

**⚠️ C++ 最常见的致命坑：**
当你写 `while (node->next != nullptr)` 时，**前提是 `node` 本身不能是 `nullptr`！**
如果链表可能为空，或者 `node` 可能走到空，你必须先判断 `node`：
```cpp
// 错误写法：如果 head 是空指针，head->next 直接段错误
while (head->next != nullptr) { ... }

// 正确写法：
while (head != nullptr && head->next != nullptr) { ... }
```
（这也是为什么配合哨兵节点 `dummy` 非常爽的原因：`dummy` 永远不是 `nullptr`，所以你可以放心大胆地写 `while (dummy->next != nullptr)`，永远不会段错误！）